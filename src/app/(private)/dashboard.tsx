import { ScrollView, Text } from "react-native";

export default function Dashboard() {
    return (
        <ScrollView>
            <Text>
                Dashboard
            </Text>
        </ScrollView>
    );
}
